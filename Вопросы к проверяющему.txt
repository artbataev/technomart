Здравствуйте!
Очень хотелось бы довести макет до самого лучшего состояния, которое возможно при моих навыках на данный момент.
К сожалению, ни один отклоненный пункт Вы не прокомментировали, поэтому не всегда могу понять, в чем мои ошибки (например, п.4 вообще вызвал недоумение).
Прошу прокомментировать непринятые пункты и указать на ошибки, чтобы я смог их исправить.

Непринятые пункты:

1) Указан подходящий vertical-align для всех блоков с display: inline-block
- vertical-align указан везде, где есть свойство display: inline-block;
Прошу указать, где именно неверно указано это свойство 

2) Прописан цвет фона для блока у которого есть фоновое изображение
Цвет фона прописан везде, где это возможно.
Случаи, где он не указан:
- .flag-new (флаг "New" у товара)
- .index-services-list: after (разделитель у табов "Сервисы" на главной странице)
- .service-details-delivery/-guarantee/-credit (содержимое блока "Сервисы")
- .modal-content-close (иконка закрытия модального окна)
Во всех этих случаях в качестве бэкграунда выступает полупрозрачное изображение в формате png, а фоновый цвет задан родительскому элементу (неясно, зачем указывать цвет родителя, а если задать другой, то из-за полупрозрачности будет виден другой фон, что не будет соответствовать макету). Можно также вырезать картинки с фоном (в случае, например, .service-details-delivery), но на мой взгляд это не имеет смысла. Можно указать transparent, но зачем? Возможно, я ошибаюсь.

3) Использованы все состояния элементов из styleguide.psd
Прошу указать, какие именно элементы не использованы (хотя бы один). Перепроверил макет, все есть из styleguide

4) У всех изображений в теге «img» прописаны размеры
В коде html у всех изображений прописаны размеры. В недоумении по поводу того, что этот критерий не принят. 
Прошу прокомментировать.

5) Все скрипты подключены непосредственно перед «/body»
В коде все скрипты находятся перед </body>, кроме скрипта подключения карты. Если Вы имели в виду именно это, то исправил. Если что-то другое, прошу прокомментировать.

6) С помощью JavaScript реализовано открытие/закрытие окна с формой (без JS-библиотек)
Не было реализовано только при нажатии на кнопку отмена. Если Вы имели в виду это, то исправил. Если требуются какие-то дополнительные доработки, прошу указать, что не сделано.

7) С помощью JavaScript добавлена анимация формы (без JS-библиотек)
Добавил.

8) Использован подход Progressive Enhancement
Прошу указать, где именно можно было использовать этот подход, но он не использован.